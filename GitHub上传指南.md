# GitHub 上传指南

## 📋 当前状态

项目已经初始化了 Git 仓库，并且已连接到远程仓库 `origin/master`。

## 🚀 上传步骤

### 1. 检查更改

```bash
git status
```

### 2. 添加所有文件

```bash
git add .
```

### 3. 提交更改

```bash
git commit -m "feat: 添加环境变量配置和完整项目文档

- 实现 API 密钥环境变量管理
- 添加完整的 README.md 项目说明
- 更新后端和前端代码使用环境变量
- 添加 API 密钥安全配置指南
- 优化代码结构和文档"
```

### 4. 推送到 GitHub

```bash
# 如果远程分支是 master
git push origin master

# 如果远程分支是 main
git push origin main

# 或者使用当前分支
git push origin HEAD
```

## ⚠️ 重要提醒

### 在推送前，请确认：

1. ✅ **所有 `.env` 文件已添加到 `.gitignore`**
   ```bash
   # 检查 .env 文件是否被忽略
   git status | grep .env
   # 如果没有任何输出，说明 .env 文件已被正确忽略
   ```

2. ✅ **没有硬编码的 API 密钥**
   - 检查代码中是否还有硬编码的密钥
   - 所有密钥都应使用环境变量

3. ✅ **`.env.example` 文件已提交**
   - 这些文件是安全的，可以提交
   - 它们不包含真实的密钥

### 如果 `.env` 文件意外被添加

如果发现 `.env` 文件被添加到暂存区，请执行：

```bash
# 从暂存区移除 .env 文件
git reset HEAD .env
git reset HEAD backend/api/.env
git reset HEAD stitch_flutter/.env

# 确认 .gitignore 已正确配置
cat .gitignore | grep .env
```

## 📝 提交信息规范

建议使用以下格式的提交信息：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

### 示例：

```bash
git commit -m "feat: 添加环境变量配置支持

- 后端使用 dotenv 管理环境变量
- 前端使用 flutter_dotenv 管理环境变量
- 添加 .env.example 模板文件
- 更新所有服务使用环境变量
- 添加 API 密钥安全配置指南"
```

## 🔍 验证上传

上传后，访问你的 GitHub 仓库，确认：

1. ✅ README.md 文件已显示
2. ✅ 所有代码文件已上传
3. ✅ `.env` 文件**没有**出现在仓库中
4. ✅ `.env.example` 文件已上传

## 🆘 常见问题

### Q: 推送时提示需要认证？

**A**: 使用以下方式之一：

1. **使用 Personal Access Token**:
   - GitHub Settings → Developer settings → Personal access tokens
   - 创建 token 并用于推送

2. **使用 SSH**:
   ```bash
   # 检查是否已有 SSH key
   ls -al ~/.ssh
   
   # 如果没有，生成新的 SSH key
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # 添加到 GitHub
   # Settings → SSH and GPG keys → New SSH key
   ```

### Q: 推送失败，提示分支冲突？

**A**: 
```bash
# 先拉取远程更改
git pull origin master --rebase

# 解决冲突后
git push origin master
```

### Q: 想撤销最后一次提交？

**A**:
```bash
# 撤销提交但保留更改
git reset --soft HEAD~1

# 完全撤销提交和更改（谨慎使用）
git reset --hard HEAD~1
```

## 📚 相关资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 文档](https://docs.github.com/)
- [Git 提交信息规范](https://www.conventionalcommits.org/)

---

**祝上传顺利！** 🎉

