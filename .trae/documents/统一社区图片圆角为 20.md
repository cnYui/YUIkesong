## 调研结论
- 我的衣柜页大图圆角：`stitch_flutter/lib/pages/wardrobe_page.dart:250` 使用 `BorderRadius.circular(20)`。
- 社区页帖子图片当前圆角：`stitch_flutter/lib/pages/community_page.dart:276` 使用 `BorderRadius.circular(8)`。

## 变更内容
- 将社区页 `_PostCard` 中图片外层 `ClipRRect` 的圆角从 `8` 调整为 `20`，与衣柜页面保持一致。

## 具体修改
- 文件：`stitch_flutter/lib/pages/community_page.dart`
- 位置：`class _PostCard` → `ClipRRect(borderRadius: BorderRadius.circular(8), ...)`
- 修改为：`BorderRadius.circular(20)`。

## 验证
- 运行 Web/Windows，打开“社区”，检查两列瀑布流的卡片图片圆角均为 20。
- 点击进入帖子详情页与返回社区页功能保持正常。